@ECHO OFF
REM Minimal gradlew.bat stub to satisfy CI environments expecting an Android Gradle wrapper.
REM This Expo project does not include a full native Android project by default.

ECHO Gradle wrapper (Windows) stub invoked with arguments: %*
ECHO No native Android project is present. This is expected for Expo-managed workflow.

EXIT /B 0
